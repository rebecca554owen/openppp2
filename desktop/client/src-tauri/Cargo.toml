[package]
name = "openppp2-client-desktop"
version = "0.1.0"
edition = "2021"

[lib]
name = "openppp2_client_desktop"
crate-type = ["lib", "cdylib", "staticlib"]

[[bin]]
name = "openppp2-client-app"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
url = "2"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
tauri = { version = "2" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"

[build-dependencies]
tauri-build = { version = "2" }
